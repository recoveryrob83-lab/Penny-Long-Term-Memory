# Engineering HQ Open Loops

Updated: 2026-07-20

## Open

| Status | Priority | Item | Next Action | Notes |
|---|---|---|---|---|
| Active | High | Department ownership architecture and dashboard inspection | Observe corrected role-routed boots and inspect demonstrated identity, ownership, or source-board defects before closing the system wrapper | ADV-20260719-044 is implemented, source verified, and closed. Engineering no longer monitors Maintenance implementation. Department Inspection remains locally validated at 414 normalized records, zero findings, and zero warnings. The remaining work is ordinary post-repair observation under `NOTE-20260717-014-department-ownership-and-dashboard-inspection.md` |
| Active | Normal | Package E: Worker Operations and Receiver Integration | Implement Slice 2: captured Worker response to semantic receiver validation and a same-row controlled outcome, with focused failure-path tests | Canonical packet: `projects/engineering/PACKAGE_E_IMPLEMENTATION_PACKET.md`. Slice 1 is implemented and live-validated: one-tab browser courier, Worker Operations cockpit, correlated response capture, return-to-HQ proof, and zero-authority self-test. Package E must not automate HQ judgment or advisory closure |
| Open | Normal | Human-readable Worker envelope summary | Add a generated display-only summary from the same `ExecutionEnvelope` before ordinary real Worker activation beyond pilots | JSON remains the authoritative machine transport representation. The summary must show Worker, task and revision, procedure and version, authorization source, verification mode, wrapper ID, run ID, and bounded instruction. It must not be independently editable, parsed as authority, or stored as a competing record |
| Paused | Low | Optional Python canonical source resolver | Do not implement unless Package E operational evidence demonstrates a concrete reliability, security, or scale gap | Python may provide defense in depth, but it is not the Worker and must not become the owner or interpreter of department judgment by default |
| Paused | Low | Canonical prompt transport verification | Do not resume the general composer investigation. Preserve the validated wrapper-marker and one-tab browser paths and change them only after demonstrated failure | Full-text equality, repeated selection, character-range, dual-witness, focus-hack, and broad timeout experiments remain out of scope |
| Active | Normal | Browser and Worker transport maintenance | Preserve exact canonical URLs, empty-composer protection, source-URL return, explicit send, one-job locking, correlated response capture, and stop-on-uncertainty behavior | The one-tab browser courier passed both command-line and dashboard self-tests. The former desktop transport remains compatibility infrastructure, not the preferred Package E path |
| Open | Normal | Dashboard auto-refresh | Add a configurable in-page auto-refresh control only if ordinary use still demonstrates value | Reuse the existing refresh path, prevent overlaps, show last and next refresh state, and preserve guarded Git sync |
| Open | Normal | Four-source dashboard observation | Confirm ordinary refresh, independent source health, cached degraded operation, and accepted scan path remain useful in real use | Do not intentionally destroy valid credentials merely to demonstrate failure mode |
| Open | Normal | Penny Inventory Worker pilot | Begin with 2–3 real sale items and verify one-row-per-item writes, stable image references, uncertainty labels, and final Sheet read | Grandfathered compatibility package remains at `workers/penny-inventory/`; evaluate it under the canonical Worker contract without treating its path as the model for new Workers |
| Open | Normal | Penny Raw Capture Worker pilot | Observe real append, canonical-target, timestamp, and verification behavior | Grandfathered compatibility package remains at `workers/penny-raw-capture/`; operational reliability still needs live use |
| Open | Normal | Reliable Connector Execution Layer | Turn the design note into an implementation packet outline aligned with the Worker run, evidence, and verification model | Drive doc: `Reliable Connector Execution Layer - Design Note` |
| Open | Normal | Operation ledger schema | Draft fields, states, idempotency keys, advisory and Worker identifiers, verification methods, partial-success handling, and recovery instructions | Align the first small schema with the canonical Worker contract, `ExecutionEnvelope`, execution-history metadata, controlled outcomes, verification views, transport integrity, and browser receipts. Do not create a second Worker run ledger |
| Open | Normal | Connector health and retry policy | Define read and write health states, bounded retry rules, stop and backoff triggers, and degraded-mode fallback | Dashboard caches, guarded Git sync, browser safety gates, scheduler policy, Worker duplicate suppression, wake suppression, backend checks, and live browser transport provide concrete examples |
| Open | Normal | Worker reliability evidence | Compare synthetic and real pilot results against `coordination/WORKER_EXECUTION_CONTRACT.md` before proposing additional Workers | `workers/WORKER_STANDARD.md` is compatibility-only. Do not create speculative Worker bureaucracy or use legacy top-level packages as the template for new profiles |
| Open | Normal | Office Leaks delivery architecture | Continue one-problem local-service-office cleanup playbooks as Business requirements mature | Preserve mechanical workflow and human-system layers |
| Open | Normal | Cost-bearing technical choices | Route hosting, subscriptions, paid APIs, tools, backend workers, queues, or paperwork overlap to Finance HQ | Finance owns money and paperwork |
| Open | Normal | Software repository strategy | Keep LifeOS as the runtime mirror and use separate repositories for educational and future standalone projects | Revisit dashboard extraction only if implementation expands materially |
| Paused | Low | Engineering HQ Daily Sync | Keep paused until Rob explicitly resumes it | Scheduler production reliability is live-validated. This unattended task remains paused by deliberate operating choice, not by an unresolved technical gate |

## Waiting / Deferred

| Status | Item | Owner / Trigger | Notes |
|---|---|---|---|
| Waiting | Additional Worker activation | Separate bounded decision by Rob and the owning department after Package E evidence | The Engineering Worker exists for bounded Engineering assignments. Any additional Worker requires one owner, authoritative profile path, allowed task class, scopes, approved tools, verification mode, lifecycle state, priority, and review condition |
| Waiting | Package E real end-to-end operations proof | Slice 2 implementation and one execution-ready Engineering advisory | Must connect canonical discovery, one-tab browser transport, response capture, semantic receiver validation, one same-row controlled outcome, duplicate protection, and Department HQ review without manual copy-and-paste reconciliation |
| Waiting | On-demand browser launch and unattended orchestration | Package E Slice 3 passes and Rob authorizes Slice 4 | Automatic launch, advisory scanning, notifications, diagnostics, and recovery must not generate authority, select priorities, or close advisories |
| Waiting | Gmail dashboard adapter | First client, multiple active leads, repeated manual inbox checking, or demonstrated risk of missing a client message | Current Gmail use does not justify another adapter |
| Waiting | General Drive dashboard adapter | Recurring client folders, proposals, deliverables, approvals, or working documents that need dashboard shortcuts | The bounded scheduler Sheet mirror is separate and implemented; general Drive browsing remains deferred |
| Waiting | Desktop window packaging | Sustained browser usefulness and a clear unattended-runtime requirement | Preferred direction remains a thin desktop shell or Windows service around the same local app |
| Waiting | Deferred prompt-launcher improvements | Concrete operational need | The old classroom launcher remains secondary. The Worker Operations cockpit now carries the active execution surface |

## Done / Recently Closed

| Closed Date | Item | Notes |
|---|---|---|
| 2026-07-20 | Package D: Operations-Procedure and Worker-Runtime Implementation | Officially closed by Rob after Slices 1–7, synthetic backend and desktop transport evidence, the Engineering Worker profile/registry/procedure, and the live `ADV-20260720-046` pilot completed with revision 1 `REPORT_AND_HOLD`, revision 2 `IMPLEMENT`, same-row receiver acceptance, and verified `IMMEDIATE_HQ` review. `ADV-20260718-042` remains open under its source owner and is not closed by this package decision |
| 2026-07-20 | Package E Slice 1: One-tab courier and Worker Operations cockpit | Rob authorized the automation UI rebuild. The browser courier preserved HQ, visited `Engineering_Worker`, captured exact correlated responses, returned to HQ, and passed command-line and dashboard zero-authority self-tests. The visible legacy prompt-and-scheduler UI is retired; its backend remains rollback-only and its scheduler is dormant by default |
| 2026-07-20 | Engineering Worker operational pilot | `ADV-20260720-046` revision 2 closed with `IMPLEMENT`, exact canonical source loading, no writes, same-row evidence, receiver acceptance, verified HQ review, and duplicate-wake suppression. Revision 1 correctly held when read scope was insufficient |
| 2026-07-20 | Engineering Worker-model reconciliation | Rob clarified that a Life OS Worker is a specialized Department-owned ChatGPT room, not an autonomous Python worker process. Handoff, README, status, open loops, and Package D packet were reconciled to make Python the courier, safety, logging, and evidence layer |
| 2026-07-19 | Engineering stale-record reconciliation | Handoff, README, status, open loops, and Package D packet were aligned to current lifecycle vocabulary, ADV-044 closure, Slices 1–7 validation, and the pending next implementation-goal decision |
| 2026-07-19 | ADV-20260719-044 shared Worker filesystem and continuity reconciliation | Life OS Maintenance HQ implemented, source verified, and closed the advisory. Shared contracts remain in `coordination/`, new profiles use `projects/<department>/workers/`, root `workers/` remains compatibility-only, departments own profiles and authority, and Engineering owns registry and runtime state |
| 2026-07-19 | ADV-20260718-042 component evidence review | Engineering mapped the advisory criteria to current code and tests. Transport integrity, semantic validation, checksums, authority, scopes, duplicate suppression, outcomes, evidence, and verification are substantially implemented as technical infrastructure. The source advisory remains open under Chief of Staff ownership |
| 2026-07-19 | Package D synthetic desktop transport validation | Rob reported exact chat navigation, active-document and composer verification, wrapper-marker verification, explicit submission, exit code 0, `durable_authority_created: false`, and exact wrapper acknowledgement |
| 2026-07-19 | Package D Slice 7 synthetic pilot validation | Focused end-to-end and receiver suite passed with 32 tests; complete dashboard regression suite passed with 222 tests and 9 warnings. Exact successful transport-history validation was added before receiver acceptance |
| 2026-07-19 | Package D Slice 6 local validation | Focused verification/runtime/Automation Logs suite passed with 20 tests; complete dashboard suite passed with 212 tests and 9 warnings |
| 2026-07-19 | Package D Slice 5 local validation | Focused receiver suite passed with 22 tests; complete dashboard suite passed with 196 tests and 9 warnings |
| 2026-07-19 | Package D Slice 4 local validation | Four focused Worker-runtime files passed with 36 tests; complete dashboard suite passed with 174 tests and 9 warnings after two narrow wording repairs |
| 2026-07-19 | Package D Slices 1–3 local validation | Worker contracts, persistence, and registry-service tests plus the full dashboard suite passed before Slice 4 began |
| 2026-07-18 | Scheduler production reliability runtime validation | Failed runs pause without blind retry; recurring Resume rearms; cloud health becomes overdue; restart handles stale one-time jobs safely; cleanup and ledger synchronization were validated |
| 2026-07-18 | Department Inspection zero-warning verification | Department Inspection reached 414 normalized records, zero findings, and zero warnings while preserving conservative detection |

Detailed historical Package D evidence remains in `projects/engineering/PACKAGE_D_IMPLEMENTATION_PACKET.md`. Active Package E scope and completion conditions are authoritative in `projects/engineering/PACKAGE_E_IMPLEMENTATION_PACKET.md`.