# Engineering HQ Open Loops

Updated: 2026-07-19

## Open

| Status | Priority | Item | Next Action | Notes |
|---|---|---|---|---|
| Active | High | Department ownership architecture and dashboard inspection | Monitor ADV-20260719-044 implementation by Life OS Maintenance HQ, then observe corrected role-routed boots before closing the system wrapper | Engineering issued ADV-20260719-044 directly from `coordination/boards/engineering.md` for the verified shared-file, Worker-pointer, profile-state, Maintenance continuity, and architecture-history repairs. Department Inspection remains locally validated at 414 normalized records / 0 findings / 0 warnings, but ordinary operation exposed meaningful semantic drift: this chat identified itself as Business HQ and nearly used the wrong source board. Approved schema: `apps/lifeos-dashboard/DEPARTMENT_INSPECTION_DATA_CONTRACT.md`. Canonical note: `NOTE-20260717-014-department-ownership-and-dashboard-inspection.md` |
| Active | Normal | Package D operations-procedure and Worker-runtime implementation | Implement Slice 5 receiver-side profile, authority, scope, parameter, source-reference, verification-mode, duplicate, and controlled-outcome validation | Implementation packet: `projects/engineering/PACKAGE_D_IMPLEMENTATION_PACKET.md`. Slices 1–4 are implemented and locally validated. Slice 4 focused tests passed with 36 tests. The first full suite reached 172 passed with two guidance-string assertion failures; Engineering repaired only the required `Open Automation Logs` and `Nothing was sent` wording contracts. Both focused regressions passed, followed by a clean full suite: 174 passed, 9 warnings in 173.76 seconds. The backend now has exact registered-title Worker transport, compact envelopes, shared pause and one-job locking, execution-history metadata, successful-send idempotency suppression, bounded failed-transport retry, manual and scheduler-triggered methods for one authorized envelope, and one-copy wrapper-ID verification. No Worker profile, real registry entry, UI, or recurring Worker authority schedule was created |
| Paused | Low | Canonical prompt transport verification | Do not resume the general composer investigation. Preserve the validated Worker-only wrapper marker path and change it only after demonstrated failure | `automation/open_worker_chat_group_verified.py` preserves exact destination, empty-composer, clipboard restoration, explicit send, and stop-on-uncertainty safeguards, then copies once after paste and checks only the expected `wrapper_id`. Historical full-text, dual-witness, repeated-selection, and character-range investigation remains abandoned. Package E and unrelated automation expansion remain deferred |
| Active | Normal | Desktop department and Worker automation maintenance | Use the validated attended HQ automation in normal operation; keep the validated Worker-only entrypoint unchanged until the synthetic pilot exercises a real UI route | Preserve exact navigation, occupied-composer protection, stable clipboard restoration, explicit send, one-job lock, verified foreground ownership without stealing composer focus, fail-closed target matching, wrapper-ID verification, and stop-on-uncertainty behavior. Existing HQ verification behavior remains unchanged |
| Open | Normal | Dashboard auto-refresh | Add a configurable in-page auto-refresh control only if ordinary use still demonstrates value | Reuse the existing refresh path, prevent overlaps, show last/next refresh state, and preserve guarded Git sync |
| Open | Normal | Four-source dashboard observation | Confirm ordinary refresh, independent source health, cached degraded operation, and accepted scan path remain useful in real use | Do not intentionally destroy valid credentials merely to demonstrate failure mode |
| Open | Normal | Penny Inventory Worker pilot | Begin with 2–3 real sale items and verify one-row-per-item writes, stable image references, uncertainty labels, and final Sheet read | Grandfathered compatibility package remains at `workers/penny-inventory/`; evaluate it under the canonical Worker contract without treating its path as the model for new Workers |
| Open | Normal | Penny Raw Capture Worker pilot | Observe real append, canonical-target, timestamp, and verification behavior | Grandfathered compatibility package remains at `workers/penny-raw-capture/`; operational reliability still needs live use |
| Open | Normal | Reliable Connector Execution Layer | Turn the design note into an implementation packet outline aligned with the new Worker run, evidence, and verification model | Drive doc: `Reliable Connector Execution Layer - Design Note` |
| Open | Normal | Operation ledger schema | Draft fields, states, idempotency keys, advisory and Worker identifiers, verification methods, partial-success handling, and recovery instructions | Align the first small schema with `coordination/WORKER_EXECUTION_CONTRACT.md`, the `ExecutionEnvelope` contract, the validated Worker execution-history metadata, and the Slice 5 controlled-outcome model |
| Open | Normal | Connector health and retry policy | Define read/write health states, bounded retry rules, stop/backoff triggers, and degraded-mode fallback | Dashboard caches, guarded Git sync, desktop automation safety gates, scheduler policy, and Worker duplicate suppression provide concrete examples |
| Open | Normal | Worker reliability evidence | Compare real pilot results against `coordination/WORKER_EXECUTION_CONTRACT.md` before proposing additional or specialized Workers | `workers/WORKER_STANDARD.md` is compatibility-only. Do not create speculative Worker bureaucracy or use the legacy top-level packages as a template for new profiles |
| Open | Normal | Office Leaks delivery architecture | Continue one-problem local-service-office cleanup playbooks as Business requirements mature | Preserve mechanical workflow and human-system layers |
| Open | Normal | Cost-bearing technical choices | Route hosting, subscriptions, paid APIs, tools, backend workers, queues, or paperwork overlap to Finance HQ | Finance owns money and paperwork |
| Open | Normal | Software repository strategy | Keep LifeOS as the runtime mirror and use separate repositories for educational and future standalone projects | Revisit dashboard extraction only if implementation expands materially |
| Paused | Low | Engineering HQ Daily Sync | Keep paused until Rob explicitly resumes it | Scheduler production reliability is live-validated. This unattended task remains paused by deliberate operating choice, not by an unresolved technical gate |

## Waiting / Deferred

| Status | Item | Owner / Trigger | Notes |
|---|---|---|---|
| Waiting | Automatic ChatGPT launch and richer recovery | Demonstrated need after current scheduling validation | Screenshots, notifications, diagnostic bundles, and automatic launch remain later work |
| Waiting | Gmail dashboard adapter | First client, multiple active leads, repeated manual inbox checking, or demonstrated risk of missing a client message | Current Gmail use does not justify another adapter |
| Waiting | General Drive dashboard adapter | Recurring client folders, proposals, deliverables, approvals, or working documents that need dashboard shortcuts | The bounded scheduler Sheet mirror is separate and implemented; general Drive browsing and shortcuts remain deferred |
| Waiting | Desktop window packaging | Sustained browser usefulness and a clear unattended-runtime requirement | Preferred direction remains a thin desktop shell or Windows service around the same local app |
| Waiting | Deferred prompt-launcher improvements | Concrete operational need | The old classroom launcher remains a secondary interface. Its dedicated `/READADVISORY`, `/CONSUMEADVISORY`, and explicit scope-metadata UI work remain captured in NOTE-007; the current Automation Command Center already carries those prompt families and scope metadata |

## Done / Recently Closed

| Closed Date | Item | Notes |
|---|---|---|
| 2026-07-19 | Package D Slice 4 local validation | Four focused Worker-runtime files passed with 36 tests. After two narrow guidance-string repairs, both failed regression tests passed and the complete dashboard suite finished with 174 passed and 9 warnings in 173.76 seconds. No functional Slice 4 regression remained |
| 2026-07-19 | Package D Slices 1–3 local validation | Rob reported the requested focused Worker contract, persistence, and registry-service tests plus the full dashboard suite passed before Slice 4 began |
| 2026-07-18 | Scheduler production reliability runtime validation | Rob live-validated the complete policy: failed scheduled runs pause immediately with one recorded failure and no retry; recurring Resume recalculates the next valid occurrence without immediate execution; cloud Health becomes `OVERDUE` after the shared five-minute grace while the dashboard is offline; restart pauses stale one-time jobs without catch-up, fake run history, or a future due time; and expired one-time Resume refuses until the definition is edited into the future. Narrow/mobile-width layout made inline action feedback easy to miss, so a transient popup remains an optional later usability improvement rather than a production blocker |
| 2026-07-18 | Overdue schedule visibility and ordering repair | Overdue one-time definitions now remain visible under the normal Scheduled filter instead of being misclassified as finished, and paused definitions with no future run sort by most recently changed rather than alphabetically. Added regression coverage |
| 2026-07-18 | Recurring schedule Resume rearm runtime validation | After reopening ChatGPT Classic, Rob resumed the paused daily LifeOS test. The schedule displayed `rearmed` with the correct next daily occurrence rather than running immediately. The first watched Resume required two button presses; no defect is declared unless the behavior repeats |
| 2026-07-18 | Failed scheduled-run pause runtime validation | Rob deliberately closed ChatGPT Classic before the daily LifeOS task. The scheduler attempted once, recorded `failed` with the correct unavailable-app reason, paused the definition, cleared Next Due, published Paused to the Google Sheet, and produced no retry during the watched interval |
| 2026-07-18 | Collapsed LifeOS project recovery runtime validation | Rob confirmed the watched draft-only run expanded the folded `Life OS` project, selected the exact Engineering HQ chat, stabilized and reacquired the destination composer, pasted the prompt, and completed successfully without sending |
| 2026-07-18 | Collapsed LifeOS project recovery implementation | Added exact collapsed-project detection, explicit ExpandPattern-only unfolding, exact target-link waiting, one bounded `Show more` recovery, destination-specific composer reacquisition, two matching content reads, and final pre-paste reacquisition. Removed the unsafe project-row invocation that could navigate to LifeOS HQ |
| 2026-07-18 | Scheduler ledger startup compaction runtime validation | Rob confirmed the deployed startup `compact` action removed the remaining blank Sheet rows even with zero local schedule definitions. Completed cleanup now removes dashboard definitions, mirror rows, and blank gaps while preserving execution history |
| 2026-07-18 | Empty-startup scheduler ledger compaction repair | Added a first-class authenticated `compact` transport action and invoked it during every dashboard startup before schedule publication. This closed the zero-definition gap that prevented old blank Sheet rows from being deleted. Added focused transport and empty-startup regression coverage |
| 2026-07-18 | Completed schedule cleanup implementation | Added a visible `Clear completed` control that targets only terminally successful one-time jobs and completed two-run debug tests, preserves run history, and deletes each definition through the existing schedule endpoint. Configured ledger deletion is now mirror-first: if the Sheet cannot confirm row removal, the local definition remains available for retry instead of leaving an orphaned cloud row |
| 2026-07-18 | Scheduler ledger endpoint live connection | Rob completed the bound Apps Script deployment and local `.env` configuration. After one visible endpoint error and correction, the Automation header displayed `Scheduler running · Ledger synced`, proving the dashboard can authenticate to and reach the no-billing Sheet endpoint |
| 2026-07-18 | No-billing scheduler ledger transport | Replaced Google Cloud service-account authentication with a secret-protected Apps Script web endpoint bound directly to the Scheduler Ledger Sheet. Added spreadsheet/tab identity checks, serialized writes, plain-text protection, redirect handling, explicit configuration errors, deployment documentation, and focused transport tests |
| 2026-07-18 | Cloud scheduler ledger implementation | Created the native `LifeOS Scheduler Ledger` Sheet and added a best-effort Google Sheets mirror with one row per schedule and live five-minute-overdue health formulas, plus schedule create/edit/pause/run/delete publication hooks, visible ledger health, local credential isolation, and focused regression coverage |
| 2026-07-18 | Completed recurrence placement UI validation | Rob confirmed the completed two-run debug definition no longer appears under Upcoming, both executions remain in Execution Record, and the bounded UI fix behaves as intended |
| 2026-07-18 | Bounded five-minute recurrence runtime validation | Rob confirmed both scheduled draft runs succeeded, the second run occurred on the five-minute repeat, the schedule card showed `Completed`, no future run remained, and no third run occurred |
| 2026-07-18 | Bounded five-minute recurrence control implementation | Added a `debug_5m` scheduler cadence with draft-only enforcement, one five-minute repeat, automatic completion after two attempts, completed-test resume refusal, visible 0/2 and 1/2 states, and focused regression coverage without changing the SQLite schema |
| 2026-07-18 | Clipboard write-verification repair runtime validation | Rob confirmed the repeated overdue draft pasted the full intended text, preserved the prior clipboard value `i`, and recorded `succeeded` in Run History. The repair keeps the prompt on the clipboard through verification and restores the original clipboard afterward |
| 2026-07-18 | Clipboard race and failure-misclassification diagnosis | Raw execution data proved ChatGPT Classic, Engineering HQ, and the composer were all verified. Clipboard verification copied only the prior one-character value, and the explicit `write_verification_failed` marker was overridden by broad keyword matching. The earlier startup-window diagnosis was rejected |
| 2026-07-18 | Overdue one-time scheduler catch-up validation | A draft-only Engineering schedule persisted while the dashboard was stopped, became overdue, fired once about 15–20 seconds after restart, recorded its result, disabled, and did not retry automatically |
| 2026-07-18 | Package C Department Inspection naming validation | Rob confirmed the dashboard displayed Chief of Staff HQ and Life OS Maintenance HQ correctly, and the focused Department Inspection suite completed with 9 passing tests |
| 2026-07-18 | Package B retired-title compatibility runtime validation | Rob confirmed a draft-only invocation using `Main Assistant HQ` translated to Chief of Staff HQ and preserved exact-destination verification without sending |
| 2026-07-18 | Package B canonical target runtime validation | Rob confirmed draft-only automation passed for Chief of Staff HQ and Life OS Maintenance HQ; existing schedules and run history displayed the correct canonical names with stable destination keys and no persisted-record migration |
| 2026-07-18 | Package B GitHub implementation | Canonical chat-title mappings, visible Automation labels, run-history and scheduler labels, mapping tests, and a bounded retired-title compatibility bridge were committed without changing stable destination keys or persisted records |
| 2026-07-18 | Department Inspection zero-warning verification | Rob confirmed 414 normalized records, zero findings, and zero warnings after notebook-status parser correction and explicit Logistics `Paused | Low` normalization; the result preserved conservative detection rather than suppressing ambiguity |
| 2026-07-18 | Department Inspection post-cleanup verification | Rob confirmed 414 normalized records, zero findings, and 13 warnings after source cleanup; all four confirmed findings disappeared without weakening detection |
| 2026-07-18 | Department Inspection first source cleanup | Separated Engineering lifecycle state from priority, removed the duplicated broad Chat HQ watch, consolidated Legacy VA folder disposition under Logistics, and removed speculative registry-reference placeholders from open-loop files |
| 2026-07-18 | Department Inspection MVP local validation | Rob confirmed the new tab works in the local Windows dashboard after guarded GitHub synchronization and server restart; live classification audit remains open under the broader ownership package |
| 2026-07-17 | Scheduled occupied-composer safety validation | A scheduled custom send to Logistics HQ found the exact destination behind `Show more`, preserved the existing draft, sent nothing, recorded `failed`, and displayed the explicit occupied-composer recovery message in Run History |
| 2026-07-17 | Automation Command Center manual Phase 1 | Eight destinations, canonical/saved/custom prompts, draft/send mode, explicit send confirmation, one-job lock, global pause, structured results, and local history implemented |
| 2026-07-17 | Saved prompt lifecycle and destination safeguards | Duplicate, save, edit, restore default destination, mismatch acknowledgement, update default, and delete validated in the UI |
| 2026-07-17 | One-time scheduling implementation | Persistent once-only jobs implemented with schedule editing, pause/resume, deletion, and completion state |
| 2026-07-17 | Recurring scheduling implementation | Persistent daily and weekly scheduling implemented in `America/Chicago` |
| 2026-07-17 | One-time Engineering live-send validation | `Hi Penny Test` succeeded and completed with no future run |
| 2026-07-17 | First daily LifeOS live-send validation | `Hi Penny LifeOS Test` succeeded and advanced to 2026-07-18 15:05 CT |
| 2026-07-17 | Mobile concurrency validation | Mobile chat activity and active response generation did not interfere with scheduled desktop execution |
| 2026-07-17 | Automation Command Center plan | Defined the original safety contract, job model, activity logging, and phased scheduling architecture |
| 2026-07-17 | Desktop department automation full validation | All seven HQs passed draft tests across visible and hidden chats, empty and occupied composers, canonical prompt insertion, clipboard verification, and draft preservation |
| 2026-07-17 | Guarded GitHub auto-sync | Clean `main` fast-forwards on load and refresh; dirty, ahead, diverged, detached, and uncertain states refuse to move |
| 2026-07-17 | Dashboard four-source validation | GitHub, Trello, Todoist, and Calendar remained healthy and live |

## Parking Lot

- Evaluate additional or specialized Workers only after the grandfathered Raw Capture and Inventory pilots and the new canonical profile architecture produce real evidence.
- Revisit software-backed capture services only if connector-based operation proves insufficient.